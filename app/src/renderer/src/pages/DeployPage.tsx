import { PagePlaceholder } from '../components/PagePlaceholder'

export function DeployPage(): React.JSX.Element {
  return (
    <PagePlaceholder title="Deploy" description="Wizard phân tích source, precheck và deploy." />
  )
}
