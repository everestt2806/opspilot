import { PagePlaceholder } from '../components/PagePlaceholder'

export function AppsPage(): React.JSX.Element {
  return (
    <PagePlaceholder title="Ứng dụng" description="Danh sách ứng dụng và phiên bản đã deploy." />
  )
}
