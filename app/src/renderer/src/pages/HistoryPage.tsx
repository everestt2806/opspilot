import { PagePlaceholder } from '../components/PagePlaceholder'

export function HistoryPage(): React.JSX.Element {
  return (
    <PagePlaceholder title="Lịch sử" description="Tra cứu deploy, migrate, rollback và cảnh báo." />
  )
}
